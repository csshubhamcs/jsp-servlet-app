services:
  tomcat:
    image: tomcat:10.1-jdk21-temurin
    container_name: directory-tomcat
    env_file: ./db.env
    ports:
      - "8090:8080"
    volumes:
      - ./target/jsp-servlet-app.war:/usr/local/tomcat/webapps/ROOT.war:ro
