# Connection settings for your own MySQL server (bring-your-own-DB mode).
# Copy this file to db.env and edit the values, then run: make start-own-db
# From a Docker container, use host.docker.internal to reach MySQL on your host machine.
DB_URL=jdbc:mysql://host.docker.internal:3306/directory
DB_USER=directory
DB_PASSWORD=directory
